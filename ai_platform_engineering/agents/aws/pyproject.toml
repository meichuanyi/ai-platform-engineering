[project]
name = "agent_aws"
version = "0.1.0"
license = "Apache-2.0"
description = "An AWS EKS AI agent using LangGraph and AWS native tools."
readme = "README.md"
authors = [
    { name = "Omar Sayed", email = "osayed@cisco.com" },
]
maintainers = [
    { name = "Omar Sayed", email = "osayed@cisco.com" },
]
requires-python = ">=3.13,<4.0"
dependencies = [
    # assisted-by Codex Codex-sonnet-4-6
    "boto3==1.40.19",
    "pydantic==2.12.5",
    "click==8.3.1",
    "python-dotenv==1.2.2",
    "a2a-sdk==0.3.0",
    "cnoe-agent-utils==0.4.0",
    "pytest==9.0.3",
    "tabulate==0.9.0",
    "keyring==25.6.0",
    "httpx==0.28.1",
    "uvicorn==0.35.0",
    "starlette==1.3.1",
    "typing-extensions==4.14.1",
    "requests==2.33.0",
    "mcp==1.27.1",
    "langchain-core==1.4.0",
    "langchain-mcp-adapters==0.2.1",
    "langgraph==1.1.6",
    "deepagents==0.3.0",
    "ai-platform-engineering-utils",
    "prometheus_client==0.23.1",
    "cryptography==48.0.1",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.hatch.metadata]
allow-direct-references = true

[tool.uv]
override-dependencies = ["protobuf==5.29.6"]
constraint-dependencies = ["nltk==3.9.4", "pypdf==6.12.0"]

[tool.uv.sources]
ai-platform-engineering-utils = { path = "../../utils" }

[tool.poetry.scripts]
agent_aws_a2a = "agent_aws.protocol_bindings.a2a_server:main"

[tool.poetry.dependencies]
uv = "^0.7.5"

[tool.poetry.group.dev.dependencies]
pytest-asyncio = "^1.0.0"
pytest-cov = "^6.1.1"
ruff = "^0.8.0"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
# Allow lines to be as long as 140.
line-length = 140
# Number of spaces per indentation level
indent-width = 2

[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
]
ignore = ["F403"]
